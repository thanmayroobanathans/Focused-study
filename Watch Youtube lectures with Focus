import React from "react";
import { BrowserRouter as Router, Route, Routes, Link } from "react-router-dom";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";

const Home = () => (
  <div className="p-6 text-center">
    <h1 className="text-4xl font-bold">Welcome to Krishna Conscious Study Hub</h1>
    <p className="mt-4 text-lg">Prepare for your exams with spiritual wisdom and motivation.</p>
    <motion.div animate={{ scale: 1.1 }} transition={{ duration: 1, repeat: Infinity, repeatType: "reverse" }}>
      <blockquote className="mt-6 text-xl italic">"Perform your duty, but do not be attached to the results." - Bhagavad Gita 2.47</blockquote>
    </motion.div>
    <Button className="mt-6"><Link to="/lectures">Explore Lectures</Link></Button>
  </div>
);

const Lectures = () => (
  <div className="p-6">
    <h2 className="text-3xl font-bold">Lecture Library</h2>
    <div className="grid grid-cols-2 gap-4 mt-6">
      <Card className="p-4">Physics Lecture 1</Card>
      <Card className="p-4">Mathematics Lecture 1</Card>
      <Card className="p-4">Chemistry Lecture 1</Card>
    </div>
  </div>
);

const Motivation = () => (
  <div className="p-6">
    <h2 className="text-3xl font-bold">Daily Motivation</h2>
    <p className="mt-4">"A person who is not disturbed by happiness and distress is eligible for liberation." - Bhagavad Gita 2.15</p>
  </div>
);

const App = () => (
  <Router>
    <div className="min-h-screen bg-yellow-100">
      <nav className="p-4 bg-yellow-500 text-white flex justify-between">
        <Link to="/">Home</Link>
        <Link to="/lectures">Lectures</Link>
        <Link to="/motivation">Motivation</Link>
      </nav>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/lectures" element={<Lectures />} />
        <Route path="/motivation" element={<Motivation />} />
      </Routes>
    </div>
  </Router>
);

export default App;
