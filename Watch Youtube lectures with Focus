<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hare Krishna Study Hub</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4e2ad;
            color: #4b2c20;
            text-align: center;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 700px;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #b36a32;
        }
        .mantra {
            font-style: italic;
            font-size: 18px;
            color: #006400;
        }
        input, button {
            width: 80%;
            padding: 10px;
            font-size: 16px;
            margin: 10px 0;
            border: 1px solid #a16c4c;
            border-radius: 5px;
        }
        button {
            background-color: #b36a32;
            color: white;
            cursor: pointer;
        }
        button:hover {
            background-color: #8a4f28;
        }
        iframe {
            width: 100%;
            height: 400px;
            border-radius: 10px;
            display: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Hare Krishna Study Hub</h1>
        <p class="mantra">"Chant and be happy! Hare Krishna Hare Krishna Krishna Krishna Hare Hare, Hare Rama Hare Rama Rama Rama Hare Hare."</p>
        <input type="text" id="video-url" placeholder="Paste YouTube Lecture URL">
        <button onclick="loadVideo()">Watch</button>
        <div id="video-container">
            <iframe id="youtube-frame" allowfullscreen></iframe>
        </div>
    </div>
    <script>
        function extractVideoId(url) {
            let regex = /(?:youtube\.com\/(?:.*v=|.*\/|.*videos\/|.*embed\/)|youtu\.be\/)([a-zA-Z0-9_-]{11})/;
            let match = url.match(regex);
            return match ? match[1] : null;
        }
        function loadVideo() {
            const url = document.getElementById("video-url").value;
            const videoId = extractVideoId(url);
            const iframe = document.getElementById("youtube-frame");
            if (videoId) {
                iframe.src = `https://www.youtube.com/embed/${videoId}?autoplay=1&rel=0&modestbranding=1&showinfo=0&controls=1`;
                iframe.style.display = "block";
            } else {
                alert("Please enter a valid YouTube URL.");
            }
        }
    </script>
</body>
</html>
